@inject NavigationManager NavManager

@code {

    protected override void OnInitialized()
    {
        NavManager.NavigateTo($"/Login?returnUrl={Uri.EscapeDataString(NavManager.Uri)}", forceLoad: true);
    }

}