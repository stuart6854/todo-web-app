@page "/projects"
@page "/projects/{ProjectIndex}"

<MudText Class="pb-4" Typo="Typo.h4">Projects</MudText>

<EditForm Model="@_newProject" OnValidSubmit="HandleCreateProject" disabled="@_isCreating">
    <DataAnnotationsValidator/>
    <MudSimpleTable Dense="true" Hover="false">
        <tbody>
        <tr>
            <td>
                <MudTextField @bind-Value="@_newProject.Name" Label="Title" Typo="Typo.input" Variant="Variant.Outlined" ShrinkLabel="true" Margin="Margin.Dense"></MudTextField>
            </td>
            <td>
                <MudTextField @bind-Value="@_newProject.Description" Label="Description" Typo="Typo.input" Variant="Variant.Outlined" ShrinkLabel="true" Margin="Margin.Dense"></MudTextField>
            </td>
            <td>
                <MudIconButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" Icon="@Icons.Material.Filled.Add"/>
            </td>
        </tr>
        </tbody>
    </MudSimpleTable>
</EditForm>

<MudTable Items="@_projects" Dense="false" Hover="false" Loading="@_isLoading" Class="mt-4">
    <RowTemplate>
        <MudTd Class="">@context.Name</MudTd>
        <MudTd Class="">@context.Description</MudTd>
        <MudTd Class="d-flex justify-end">
            <MudIconButton Size="Size.Small" Color="Color.Error" Icon="@Icons.Material.Filled.Delete"/>
        </MudTd>
    </RowTemplate>
</MudTable>